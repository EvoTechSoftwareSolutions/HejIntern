# Frontend (React + Vite)

Quick starter for the React frontend using Vite.

Install and run:

```bash
cd frontend
npm install
npm run dev
```

The dev server runs on `http://localhost:3000` and proxies `/api` to the backend at `http://localhost:4000`.
