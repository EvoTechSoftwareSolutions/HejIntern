# Backend (Express)

Quick starter for the Express backend.

Install:

```bash
cd backend
npm install
```

Run in development (requires `nodemon`):

```bash
npm run dev
```

Run production:

```bash
npm start
```

Health: `GET /api/health`
Message: `GET /api/message`
